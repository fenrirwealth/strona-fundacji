import { impact } from '@/lib/content';
import { Counter, Reveal } from './motion';
export function Impact() {
  return <section id="wplyw" className="impact-section section-space"><div className="shell"><Reveal className="section-heading"><div><p className="eyebrow">01 / Nasz wpływ</p><h2>Za liczbami<br /><em>stoją ludzie.</em></h2></div><div className="section-intro"><p>Zaangażowanie społeczne wymaga mierzalnych celów. Liczba beneficjentów, zakres wsparcia i wartość przekazanej pomocy tworzą podstawę rozmowy o rezultatach.</p><p className="editorial-note">Dane demonstracyjne — układ do uzupełnienia na podstawie zatwierdzonego raportu. Nie przedstawia wyników Fundacji.</p></div></Reveal><div className="impact-grid">{impact.map((item, i) => <Reveal key={item.label} delay={i * .1} className="impact-item"><p className="metric"><Counter value={item.value} suffix={item.suffix} /></p><h3>{item.label}</h3><p>{item.note}</p></Reveal>)}</div><div className="impact-foot"><span>JASNY ZAKRES · MIERZALNE CELE · PRZEJRZYSTE PODSUMOWANIE</span><a href="/o-fundacji" className="text-link">Poznaj Fundację <span aria-hidden="true">↗</span></a></div></div></section>;
}
