import { impact } from '@/lib/impact';
import { Counter, Reveal } from './motion';
export function Impact() {
  return <section id="wplyw" className="impact-section section-space"><div className="shell"><Reveal className="section-heading"><div><p className="eyebrow">01 / Nasza pomoc</p><h2>Za liczbami<br /><em>stoją ludzie.</em></h2></div><div className="section-intro"><p>Od początku działalności pomagamy dzieciom, rodzinom oraz placówkom. Poniższe wartości są szacunkowym podsumowaniem całej dotychczasowej pomocy Fundacji.</p><p className="editorial-note">Dane szacunkowe, obejmujące również dzieci objęte akcjami paczek świątecznych.</p></div></Reveal><div className="impact-grid">{impact.map((item, i) => <Reveal key={item.label} delay={i * .1} className="impact-item"><p className="metric"><Counter value={item.value} prefix={item.prefix} suffix={item.suffix} /></p><h3>{item.label}</h3><p>{item.note}</p></Reveal>)}</div><div className="impact-foot"><span>DANE SZACUNKOWE · OD POCZĄTKU DZIAŁALNOŚCI</span><a href="/przejrzystosc" className="text-link">Sprawdź dane Fundacji <span aria-hidden="true">↗</span></a></div></div></section>;
}
