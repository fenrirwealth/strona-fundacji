import { campaigns } from '@/lib/content';
import { Reveal } from './motion';
/** A real link covers each card, so the hover treatment also works for keyboard focus. */
export function Campaigns() {
  const topics = { '01': 'wyprawka-szkolna', '02': 'paczka-swiateczna', '03': 'biezace-potrzeby' } as const;
  return <section id="dzialania" className="section-space shell"><Reveal className="section-heading"><div><p className="eyebrow">02 / Jak możesz pomóc</p><h2>Wybierz sposób,<br /><em>który jest Ci bliski.</em></h2></div><p className="section-intro">Możesz dołączyć do akcji szkolnej, spełnić świąteczne marzenie dziecka albo zapytać nas o aktualne potrzeby. Każda forma pomocy ma znaczenie.</p></Reveal><div className="campaign-grid">{campaigns.map((campaign, i) => <Reveal key={campaign.id} delay={i * .12}><article className="campaign-card"><a href={`/kontakt?temat=${topics[campaign.id as keyof typeof topics]}#formularz`} aria-label={`${campaign.detail}: ${campaign.title}`}><div className="campaign-image"><img src={campaign.image} alt={campaign.alt} width="640" height="480" loading="lazy" /><span className="campaign-number">{campaign.id}</span><span className="campaign-hover-caption" aria-hidden="true">TWÓJ GEST.<br />REALNA POMOC.</span><span className="campaign-arrow" aria-hidden="true">↗</span><span className="campaign-image-note">{campaign.imageNote}</span></div><div className="campaign-body"><p className="eyebrow">{campaign.category}</p><h3>{campaign.title}</h3><p>{campaign.description}</p><span className="text-link">{campaign.id === '03' ? 'Zapytaj, jak możesz pomóc' : 'Chcę pomóc'} <span aria-hidden="true">↗</span></span></div></a></article></Reveal>)}</div><div className="archive-link"><p>Zobacz zdjęcia i materiały z wcześniejszych działań Fundacji.</p><a href="/archiwum" className="text-link">Przejdź do archiwum ↗</a></div></section>;
}
