import { campaigns, partnershipMail } from '@/lib/content';
import { Reveal } from './motion';
/** A real link covers each card, so the hover treatment also works for keyboard focus. */
export function Campaigns() {
  return <section id="dzialania" className="section-space shell"><Reveal className="section-heading"><div><p className="eyebrow">02 / Projekty strategiczne</p><h2>Dwa kierunki.<br /><em>Wspólny sens.</em></h2></div><p className="section-intro">Program edukacyjny i zaangażowanie świąteczne. Dwa obszary, wokół których możemy określić zakres partnerstwa, zaangażowanie zespołu i oczekiwane rezultaty.</p></Reveal><div className="campaign-grid">{campaigns.map((campaign, i) => <Reveal key={campaign.id} delay={i * .12}><article className="campaign-card"><a href={`${partnershipMail}%20%E2%80%94%20${encodeURIComponent(campaign.title)}`} aria-label={`${campaign.detail}: ${campaign.title}`}><div className="campaign-image"><img src={campaign.image} alt={campaign.alt} width="640" height="480" loading="lazy" /><span className="campaign-number">{campaign.id}</span><span className="campaign-hover-caption" aria-hidden="true">WSPÓLNY CEL.<br />KONKRETNY PROJEKT.</span><span className="campaign-arrow" aria-hidden="true">↗</span><span className="campaign-image-note">{campaign.imageNote}</span></div><div className="campaign-body"><p className="eyebrow">{campaign.category}</p><h3>{campaign.title}</h3><p>{campaign.description}</p><span className="text-link">Porozmawiajmy o współpracy <span aria-hidden="true">↗</span></span></div></a></article></Reveal>)}</div><div className="archive-link"><p>Dokumentacja wcześniejszych działań i materiałów Fundacji.</p><a href="/archiwum" className="text-link">Przejdź do archiwum ↗</a></div></section>;
}
